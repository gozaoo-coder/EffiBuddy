<template>
  <router-view />
</template>

<script setup lang="ts">
// App.vue is the shared root used when a single Vue app drives multiple
// routes. Each window entry (windows/*.ts) mounts its own view directly
// instead, so this component is reserved for future unified-shell usage.
</script>
