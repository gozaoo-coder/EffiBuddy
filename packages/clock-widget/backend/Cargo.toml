[package]
name = "clock-widget"
version = { workspace = true }
edition = { workspace = true }
description = "Clock widget plugin backend"

[lib]
name = "clock_widget"
crate-type = ["cdylib", "rlib"]

[dependencies]
plugin-sdk-rs = { workspace = true }
chrono = "0.4"
serde_json = "1"
