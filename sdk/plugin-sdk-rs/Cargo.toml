[package]
name = "plugin-sdk-rs"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for Desktop Suite plugins (PluginTrait + CoreContext + macros)"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
